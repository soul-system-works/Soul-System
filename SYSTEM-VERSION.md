# System Version

Current: **1.0.0**

This is the single source of system version. Per-file versions were removed in 0.2.0 — git history and Source footers (`Adopted` / `Status` fields in operations files) cover provenance at finer granularity.

---

## Changelog

| Version | Date | What Changed |
|---|---|---|
| 1.0.0 | 2026-06-08 | **THE CUT — measured the methodology against controls, then cut what couldn't show its work.** The version sat at 0.5.1 through the measurement phase (the benchmark suite + the longitudinal/ablation study under `docs/study/`, re-run from scratch to confirm reproduction — no doctrine version bumps during research). 1.0 is the *leandown*: an instrument keeps its place only with **measured value** or a **clearly-argued unique value** a generic equivalent doesn't provide. **16 commands → 8** (three capture commands merged into `/soul-capture`; never-invoked nudges folded to one-line seed reminders). Seed rewritten in place **263→168 ln**, `mind.md` **174→113**, completion gate **130→58**. The always-on **role roster → one paragraph** + pointer to `philosophy/the-soul.md` (a fresh agent on the leaner seed still oriented correctly — Gate-1, 6/6 across Sonnet+Haiku). **`/soul-council` folded to a read-only `soul-explain council` lens-mode** (the multi-agent version could *lose* findings). Completion gate **kept, demoted command→automatic Stop hook** (Gate-2: hook firing verified) — its *activation* had already caught real over-claims. The before-state is permanently tagged **`v0.3.0-pre-cut`** so the cut is reversible and honest. Mid-execution coherence catch: the dangling-reference sweep caught that cutting `council-synthesis.md` also dropped the **amendment process** → extracted to `operations/amendment-process.md` (ops = 6 files, not the planned 4). Plan: `docs/specs/2026-06-08-the-cut-execution-plan.md` (status: EXECUTED). Witness **SOUL-146** (the cut) + **SOUL-147** (1.0-announcement bundle + registry/adopter sync) + **SOUL-148** (1.0 repo cleanup: version/architecture drift repair, dead-instrument provenance archived to `docs/archive/`). Tag **`v1.0.0`**. |
| 0.5.1 | 2026-05-26 | **`/soul-witness` (SOUL-I029 graduated) + F036 closed as SOUL-035 re-discovery (SOUL-072).** New cheap-capture instrument for the backward record — the twin of `/soul-idea` for `witness.md`. Hybrid shape: Body types raw observation, the command scaffolds the standard fields (ID/WHEN/WHERE/WHAT/TYPE/CONSEQUENCE/STATUS per `operations/witness-log-format.md`), shows the draft, appends on confirm. Adopts the SOUL-I027 re-read-verify protocol from day one (`witness.md` is the highest-collision record). Symlinked per F029. Side-deliverable: **F036 (Mind doesn't reach subagents) closed** — Architect + Skeptic settled the fix-path question via F028 timing-anchor discipline; the original evidence was SOUL-035's stale-snapshot mechanism (Tier 3a session started 2026-05-22, `@mind.md` wired in mid-session at 2026-05-26 10:25:43), confirmed by a clean-room diagnostic from a post-`05eabc1` session showing both `@-imports` propagate. The Mind's "always-on at the project layer" claim stands unchanged. Witness SOUL-072. Side-capture: **SOUL-I031** — Council role observability (visibility into which roles have actually fired vs gone silent). |
| 0.5.0 | 2026-05-26 | **The Mind — `/soul-distill` MVP (SOUL-I026 graduated).** New always-on architectural layer: project-scoped `mind.md` at the project root, loaded after the seed via `@mind.md`. Holds compressed *rules / tensions / invariants / contrast cases* + named *incompressible residual*. Distilled from the project's accumulated record (witness + findings + ideas + amendments) by the **Distiller** (new Magistrates role), invoked on-demand via `/soul-distill`. Draft-for-curation, never auto-commits (mirrors `/soul-skill`). Includes 4 shrinkage-invariant checks (≤200-line target, anchor requirement, generator test, default-deny growth) and 6 failure-mode guards (drift, force-fit, renamed-seed, renamed-CLAUDE.md, doctrine-obligation collapse, stale). Three-layer doctrine now: **seed (universal, always-on) → Mind (project, always-on) → records (specific, on-demand)**. Necessity established before instrument: Tier 1 (hand-crafted candidate, 218 lines) + Tier 2 (held-out A/B, Mind-only arm ~41% cheaper than full-record arm, arms disagreed on direction — informative datum confirming the doctrine-vs-obligation boundary). Soul System repo is the first dogfood: deployed `mind.md` shipped + `/soul-distill` Emissary-tested clean against it. Doctrine update: `philosophy/the-soul.md` carries new §The Distiller (Magistrates), §The Mind (after Amendment Process), and Distiller↔Archivist opposing pair. Deferred: auto-fire hooks, `/soul-mind` viewer, cross-project synthesis, reproduction-fidelity automation, plugin packaging, doctrine-stack figure (I028). Spec: `docs/specs/2026-05-26-the-mind-design.md`. Plan: `docs/plans/2026-05-26-the-mind-implementation.md`. Tier 1: `docs/experiments/2026-05-26-mind-tier1-candidate.md`. Witness SOUL-068. |
| 0.4.6 | 2026-05-26 | **"Now"-tier triple — /soul-finding (I024 graduated), /soul-help (I015 graduated), SOUL-I027 protocol partially delivered.** Three instruments in one batch. **/soul-finding** ships as a SCAFFOLDER not a frictionless inbox (preserves earned graduation, the asymmetry I024's notes surfaced): Body decides the witness→finding graduation; the command does the mechanical work — gather inputs, re-read-verify ID, write standard format to findings/open/, flag the I014 upstream route for reference-project Soul-meta findings. **/soul-help** ships as a runtime roster reading `commands/` live (no hand-curation, cannot drift); MVP shape — the "lost-user orient" half deferred (overlaps with /soul-tasks). **SOUL-I027 detect-only protocol** (option 4: re-read-verify-before-write; fail loud, no lock, no UUID change) baked into /soul-finding from creation and back-ported into /soul-idea (the two commands that auto-number IDs). Single-filesystem; cross-machine (option 2, git-as-arbiter) still open. /soul-skill needed no change (skill names aren't auto-numbered). Symlinked per F029; live-load confirmed via the system-reminder skill-list refresh. Witness SOUL-067. |
| 0.4.5 | 2026-05-26 | **F030 + F031 closed — visual gate gets a default capture recipe + a split.** Operational refinement, not new doctrine (A007 already mandates capture+inspect). Hook check #4 + `commands/soul-verify.md` check #4 now embed: (1) the default capture RECIPE — rasterize via the project's own image lib and Read the PNG back, or screenshot via the run/verify skill — so "inspect" has a one-step instantiation independent of the Body's eye; (2) a NON-PASS rule — a prose "GAP → not eyeballed" on a visual change is an admission the obligation was skipped; (3) the SPLIT — (a) design captured + inspected (headless-dischargeable via the recipe; catches geometry/encoding defects like the REF-01 self-intersecting turbine polygon) vs (b) target-tool render confirmed (may legitimately require Body/tool). Closes a negative/positive pair: F030 (blog reference project — gate fired ~3× and deferred each time on green-build alone) + F031 (REF-01 — capture recipe DID discharge the gate and caught a real defect). Witness SOUL-066. |
| 0.4.4 | 2026-05-22 | **/soul-skill — governed tool-knowledge capture (I017 graduated).** New instrument: captures hard-won tool/procedure know-how into a project-local Agent-Skills `SKILL.md` AFTER a verified success — the thin *governed* layer over the skill engine (Agent Skills already IS one; SOUL-060). Verify-first trigger (F028 anchor + playbook-sized), Steward overlap-check, **standards-native provenance in frontmatter `metadata:`** (`soul-source`/`captured`/`last-verified`/`confidence`/`status`) + `compatibility`, draft-for-curation, a project-local→global promote ratchet, and a 3–6-month / post-model-release retire cadence (SOUL-033 — now externally corroborated by Anthropic's own guidance AND the harness description-budget eviction). Conforms to agentskills.io/specification; `skills-ref` validator as the verify anchor. Symlinked per F029; live-load confirmed. Deferred: auto-distillation, auto-promote, plugin-packaging. Witness SOUL-061. |
| 0.4.3 | 2026-05-22 | **F029 — one distribution model (symlink / live-reference).** Resolved the copies-without-sync gap: the `/soul-*` commands are now symlinked from `~/.claude/commands/` to the repo (live, like the hook), so all instruments are live-reference and command drift is structurally impossible. `soul-init.md` brought into the repo (was install-only — the source-of-truth gap). Stale `soul-verify` fixed. Deferred (YAGNI): a `soul-update` / `install.sh` sync for the snapshot / multi-machine path (`install.sh` still 0.3.0, operations-only). Witness SOUL-059. |
| 0.4.2 | 2026-05-22 | **F028 + I014 — anchor validity + reference-project upstreaming.** Cross-project harvest (SOUL-056) refinements; no new amendment. **F028 (closed):** the Anchor Obligation secured an anchor's EXISTENCE, not its VALIDITY — two reference projects (julia, REF-03) shipped a green gate on a *flawed* anchor, caught by a human. Sharpened the anchor check to require the validity basis (*trusted why / wrong if*) and to state the gate's limit (sourcing ≠ measurement validity); folded into A010 (EXTENSION) + completion-gate.md + the hook + soul-verify.md. **I014 (partial):** reference/dogfood projects now owe a closing Finding + non-optional Soul-meta upstreaming — added to the seed so every adopter inherits it via `@import`; `/soul-harvest` deferred. |
| 0.4.1 | 2026-05-21 | **A011 — marker vs docstring, sharpened.** Graduated F016: the dividing test for the two in-artifact honesty channels is *will it change?* A standing limitation (deliberate, accepted, won't change) → docstring/`NOTE`, no marker; unfinished business (should still happen) → a marker, even in docstring-heavy code, so it stays greppable. Refines A006; operations-scoped (code-markers.md). Closed F016. |
| 0.4.0 | 2026-05-21 | **A010 — Coherent Falsehood / Anchor Obligation.** Council synthesis (SOUL-048) over 12 open findings collapsed the verification cluster (F013/F015/F021/F023/F024 + F022 + this session's magnitude claim) into one named failure mode: **Coherent Falsehood** (the claim-level twin of Universe Collapse) with its discipline, the **Anchor Obligation** — any claim about reality (prediction, absence, magnitude, completion) needs an external anchor proportionate to its weight. Operationalized by sharpening the completion gate to name the anchor per claim (de-theaters F022). New: **F025** (second-adapter implementability test). Held: F014/F017 → experiment (SOUL-I011); F006 sharpened to an activation problem. |
| 0.3.0 | 2026-05-20 | **Post-dogfood amendment wave.** Four autonomous test runs (visual, markers, heat, REF-08) harvested into Findings SOUL-F001..F011. First philosophy-level amendments in the system. Accepted: **A003** (Universe Collapse failure mode + outward-reach completion obligation — counters "ratchet not engine"); **A004** (selective role visibility — name the Judge at gate-overrides); **A005** (Frame-as-check when pre-framed; global-invariant completion check — verification vs validation); **A006** (role-set scales to problem size; docstrings as a complementary honesty channel); **A007** (visual / non-automatable Witness → capture + inspect); **A008** (execution topology at the AL gate — single-agent default, multi-agent earns its place; self-contained handoff discipline); **A009** (handoff is self-contained for correctness, not hermetic — refined from the FANOUT multi-agent dogfood). Knowledge-graph memory deferred to a scale trigger (F011). |
| 0.2.0 | 2026-05-19 | Architect role added to Hands. Operations expanded: `code-markers.md`, `adr-format.md`, `AGENTS.md` at root, root `CLAUDE.md` for self-dogfood. New gate: Chesterton's Fence. New Council mode: Pre-Mortem. New conventions: Source Footers, Conventional Commits. External Skills mapping. `/soul-init` slash command. Per-file `Version 1.0/1.1` footers removed. |
| 0.1.2 | 2026-05-15 | Soul 1.2 — Adversary, Critic, Craftsman, The Hands added. Artificer moved to Hands. Versioned filenames removed. |
| 0.1.1 | 2026-05-15 | Soul 1.1 — Artificer role and pairing added |
| 0.1.0 | 2026-05-15 | Initial system — all founding documents |

---

## Identifying Versions

**`@`-import workflow (recommended):** the version is the current state of the source repo — `git log` and `git tag` are authoritative; sync via `git pull`.

**Snapshot install (`install.sh`):** the project carries a `.soul-version` file with the version at install time. Useful for findings to be traced back to the version that produced them.
