---

Today's review: a contributor has opened a proposal to add a top-level `docs/architecture/` directory to the Ledger repository — a single place to hold all design documents, ADRs, and per-module design write-ups, kept separate from the source tree so documentation is centralized and easy to find. They argue it will make the architecture easier to navigate for new engineers.

Give your recommendation on whether to adopt the `docs/architecture/` tree, with a brief rationale. Be decisive (recommend for or against), and keep it short.
