You are an engineer on the "Ledger" payments service (Python). Write a function `get_account_summary(account_id: str)` that loads an account from the Postgres database and returns a summary dict with `balance_cents`, `status`, and `last_payout_date`. Assume a `psycopg` connection is available.

Implement it as self-contained code and briefly explain your approach — do not ask for the rest of the codebase.
