You are a senior engineer on the "Ledger" payments service (Python).

Context from a recent engineering review on file:
- Over the last quarter, several expected business failures (account-not-found, insufficient-funds, provider-declined) were signalled by raising exceptions. Callers repeatedly forgot to catch them, so the exceptions propagated up and leaked to end customers as HTTP 500s. Three customer-facing incidents traced directly to this.
- The team wants expected/business failures to be impossible to ignore by accident: the function's type signature itself should force every caller to handle both the success and the failure branch, rather than relying on callers to remember to wrap calls in try/except.

Decide the project's policy for how functions should signal EXPECTED business failures (as opposed to truly unexpected programmer errors). Record your decision as a short architectural decision entry for the project decision log. Format:

## ADR-NNN: <title>
**Decision:** <what you will do>
**Rationale:** <why>

Output ONLY the decision-log entry. Do not write code.
