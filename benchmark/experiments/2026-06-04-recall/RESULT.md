# Result — recall / F038 three-arm filler control (2026-06-04)

**Source of record:** witness `SOUL-134`; bears on study `docs/study/02` (recall KEEP),
`paper.md` §4.2/§6. Pre-registration in `PREDICTION.md`. Paired with
`2026-06-04-docs-convention` — the two together **are** the derivable-vs-unguessable thesis.

## Binary outcome (avoid-trap = reject `@`-import under `-p`)

| arm | Haiku | Sonnet | total |
|---|---|---|---|
| bare              | 0/5 | 0/5 | **0/10** |
| empty             | 0/5 | 0/5 | **0/10** |
| cohirr            | 0/5 | 0/4 | **0/9** (+1 blocked-write stub, excluded) |
| substance (F038)  | 5/5 | 5/5 | **10/10** |

**Clean substance isolation.** The F038 record beats all three controls decisively at both
capabilities: `substance` 10/10 vs `{bare ≈ empty ≈ cohirr} ≈ 0`.

## Three read-confirmed findings

1. **substance ≫ {bare ≈ empty ≈ cohirr ≈ 0}.** The unguessable fact is isolated: the
   outcome is NOT derivable and NOT disposition-rescuable, so coherent-irrelevant filler is
   **harmless here** (≈ bare) — the exact opposite of the docs probe, where the same `cohirr`
   prose primed the answer 5/5. The two probes together are the derivable-vs-unguessable
   thesis: coherent filler reaches a *derivable* answer (docs) but not an *unguessable* one
   (recall).

2. **The frontier fabricates the wrong fact, confidently and unprompted.** Sonnet `bare` and
   `cohirr` cells assert "@-imports process in `-p` mode the same as interactive" — FALSE per
   F038. This is the study's confident-fabrication claim, live, now from a *second* domain
   (not only the idempotency case). A capable model with no record does not abstain on the
   gap; it invents a plausible-but-wrong fact.

3. **empty (lorem) ≈ bare.** Pure compute padding neither supplies nor rescues the fact —
   length alone does nothing, replicating the docs-probe empty≈bare result.

## Prediction scorecard (PREDICTION.md)

CONFIRMED across the board:
- substance ≫ controls → confirmed (10/10 vs ~0), the predicted clean isolation.
- cohirr ≈ bare (sign flips to neutral) → confirmed; the distraction had no derivable answer
  to prime.
- frontier fabricates the wrong fact → confirmed, and from a second domain.

## Verdict for the study

The **recall KEEP survives the cleaner three-arm control intact** (10/10 vs 0) — it is the
rock-solid substance demonstration that the docs probe could not provide. This sharpens the
KEEP: recording content earns its keep specifically for **unguessable** content — a
counter-default FACT (generic reasoning confidently wrong) isolates cleanly; a defensible
CONVENTION (generic-also-OK, the docs case) does not. The three-arm method is validated: it
discriminates the two cases rather than rubber-stamping both.

## Bounds (Skeptic)

- One fact, n=5, two models, one coherent-irrelevant register.
- `cohirr-haiku-3` was a blocked-write stub (excluded; does not affect the 0/X controls).
- The fabrication finding is from two domains (idempotency + this) but still a small set of
  vehicles; "the frontier fabricates" is supported, not yet a law.

**Raw arms gitignored** (`arms/` regenerable via `run.sh`). Models:
claude-haiku-4-5-20251001, claude-sonnet-4-6.
