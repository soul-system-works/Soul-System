```
AMENDMENT ID:    SOUL-A016
DATE:            2026-05-27 (drafted)
WITNESS IDS:     SOUL-098 (the BMAD Researcher beat — first anchor:
                 BMAD's actual architecture contradicts the seed's
                 peer-role-mapping framing);
                 SOUL-099 (the 10-role /soul-council convening — the
                 chamber's synthesis IS this amendment's blueprint;
                 walked Magistrates [Revelator, Archaeologist,
                 Emissary, Steward, Seer] + Tribunes [Skeptic,
                 Accountant, Advocate] + Censors [Guardian,
                 Cartographer]);
                 SOUL-100 (this amendment's filing, plus the
                 reshape of F041 from "phase-vs-posture partial
                 mapping" to "Coherent Falsehood in seed doctrine").

WHAT CHANGES:    Replace the slipped paragraph at
                 `operations/CLAUDE.md:208` with two paragraphs
                 that name Soul's actual relationship to external
                 workflow tools (R3 hybrid — wrapping primary,
                 parallel possible) using class-only framework
                 references rather than specific BMAD/CrewAI/AutoGen
                 names.

                 **Current text** (`operations/CLAUDE.md:208`):

                 > **Roles in the Soul System are perspectives, not
                 > distinct agents.** A project using a multi-agent
                 > framework (BMAD, CrewAI, AutoGen) can adopt the
                 > Soul System without restructuring its agents: a
                 > BMAD Analyst agent can embody the Witness role,
                 > a BMAD Architect agent can embody the Soul's
                 > Architect, and so on. The Soul layers above how
                 > you already work.

                 **Proposed replacement** (two paragraphs replacing
                 the one):

                 > **Soul composes; it does not replace.** Soul is
                 > the project's **lifetime layer** — doctrine that
                 > loads into every session (the seed + `mind.md`)
                 > and record that accumulates across them
                 > (`witness.md`, `findings/`, `amendments/`). Multi-
                 > agent frameworks, IDE rules, test-driven workflows,
                 > and your own conventions are narrow workflow
                 > capacity used *within* that layer.
                 >
                 > **Soul wraps; it does not peer-map.** A tool's
                 > named agents (e.g. an "Analyst" or "Architect"
                 > persona) are the tool's phase-tools; Soul's
                 > continuous-posture roles (Witness, Tribunes,
                 > Censors) operate at a different layer and are
                 > not equivalents. Across the project's lifetime,
                 > only the Soul layer persists — tool sessions come
                 > and go, their outputs feed Soul's record. Inside
                 > any single session, Soul's gates (Counterweight,
                 > Anchor Obligation, the completion gate) fire
                 > alongside whatever tool work is happening; both
                 > layers operate, neither replaces the other. The
                 > Surface-when… table above maps tool capacity to
                 > Soul roles by **shape match at the right moment**,
                 > not by embodiment.

                 **No other edits in this amendment.** The first
                 paragraph of §External Skills and Tools (line 191)
                 already says *"The Soul System is a meta-layer. It
                 composes with other skill ecosystems, methodologies,
                 and tools rather than replacing them."* — that line
                 is canonical and stays. The Surface-when… table
                 (lines 195–204) stays as-is; its content is
                 already shape-match, not embodiment. The closing
                 paragraph (line 210) about Claude Code skills + AGENTS.md
                 also stays unchanged.

WHERE IN SOUL:   - `operations/CLAUDE.md:208` — the targeted line.
                   Single-paragraph replacement (1 paragraph → 2
                   paragraphs); net growth ~6–8 lines.
                 - No `philosophy/the-soul.md` edit (the philosophy
                   does not currently describe external-tool
                   composability — the seed is the authoritative
                   surface for this topic).
                 - No `mind.md` edit at this beat (the phase-vs-
                   posture distinction is candidate Mind material;
                   defer to the next /soul-distill per chamber
                   recommendation, not part of A016).
                 - No `README.md` edit (already canonical at line 17).
                 - No `AGENTS.md` edit (already canonical at lines 7-9).

QUESTION ONE — EVIDENCE:

                 **Three independent anchors confirm the slip is
                 real:**

                 1. **Three project artifacts vs one.** README +
                    AGENTS.md + implicit mind.md architecture all
                    use stack-layered framing ("composes, does not
                    replace; layers above how you already work").
                    Only the seed line 208 uses peer-role-mapping
                    framing. The seed is the outlier within the
                    project's own canon.

                 2. **Field analogs (SOUL-099 Archaeologist),
                    bounded survey.** Of the comparable systems the
                    chamber explicitly surveyed — Cursor rules,
                    Continue.dev rules, Aider conventions, AGENTS.md
                    cross-vendor convention, the Claude Agent Skills
                    platform itself (N=5) — **all** use *project-
                    layer doctrine + tool-layer execution* and
                    **none** claim peer-role-mapping with the tools
                    they coexist with. **Bound (per A010 absence-
                    claim discipline):** this is "no analog in 5
                    surveyed systems," not "no analog anywhere in
                    the field" — some niche framework could use
                    peer-role-mapping. If one surfaces, anchor 2
                    weakens; anchors 1 (the project's own README
                    vs seed disagreement) and 3 (BMAD's actual
                    architecture) remain independently sufficient.

                 3. **BMAD's actual architecture (SOUL-098).** First
                    direct external test of the seed's claim against
                    a specific framework: BMAD agents are skill-
                    invocations in fresh chats with serial handoff
                    between phase-aligned agents — process-shaped,
                    not perspective-shaped. Soul's continuous-
                    posture roles have no BMAD counterpart. Reference
                    grounding: `references/bmad-method-v6-2026.md`.

                 The chamber's *load-bearing* evidence is anchor 1
                 (the seed disagrees with the project's own README
                 and AGENTS.md, in the same week the section was
                 adopted — Seer's timeline read at SOUL-099). Even
                 if anchor 3 hadn't fired, the seed-vs-README
                 disagreement alone is sufficient to identify the
                 slip; anchor 3 just adds the field reality test.

                 **Validity caveat applied (per F028 + F040):** The
                 BMAD-specific quotes inside `bmad-method-v6-2026.md`
                 are secondary-reading via WebFetch's model
                 summarization, explicitly flagged in the sidecar.
                 The structural conclusion (anchor 3) rests on
                 *triple-source convergence* across docs.bmad-
                 method.org, github.com/bmad-code-org/BMAD-METHOD,
                 and bennycheung.github.io — not on any single
                 verbatim quote. Anchors 1 and 2 do not depend on
                 anchor 3 at all.

QUESTION TWO — NECESSITY:

                 - **The Coherent Falsehood is in always-on
                   doctrine.** The seed loads into every session
                   via project CLAUDE.md @-import. Every dogfood
                   project that adopts via @import inherits the
                   slipped framing. The propagation surface is
                   the broadest in the system.

                 - **Cost of fix is bounded** (SOUL-099 Accountant):
                   ~5–8 lines of seed text replacement; this
                   amendment's drafting (already done); ~30 min
                   for Body to review and accept; ~5 min to land
                   the seed edit. No downstream propagation needed
                   (README and AGENTS.md already correct).

                 - **Cost of not fixing is ongoing** (SOUL-099
                   Advocate): every adopter who reads the seed
                   before the README walks away with a wrong
                   mental model that they will have to unlearn the
                   first time they actually try to apply Soul on
                   top of a tool. "Analyst embodies Witness"
                   actively misleads — it suggests renaming agents,
                   tagging prompts, or otherwise restructuring the
                   tool, none of which is what Soul actually
                   requires.

                 - **The README/AGENTS.md framing needs canonical
                   recognition.** Without the amendment, the seed
                   contradicts the public-facing artifacts —
                   doctrine inconsistency the project's own
                   coherence-under-independent-reading test (Mind
                   rule 11) cannot survive.

                 - **F041 cannot close until the seed is fixed.**
                   The finding's discharge path is this amendment;
                   without A016, F041 stays open indefinitely
                   waiting on a CrewAI/AutoGen second-framework
                   test that the chamber determined is *not*
                   load-bearing for the framing-correction.

QUESTION THREE — COHERENCE:

                 - **Aligns with README.md:17** (the README's
                   "composes, does not replace" framing is now
                   in the seed too).

                 - **Aligns with AGENTS.md:7-9** (the AGENTS.md
                   "layer that sits above" framing is now in the
                   seed too).

                 - **Aligns with mind-rule-11** ("Two parties
                   reading the same record must arrive at the same
                   meaning") — the seed and the README will now
                   actually agree; coherence-under-independent-
                   reading restored.

                 - **Aligns with mind-rule-2** (default simplicity)
                   — the rewrite uses the README's existing words
                   rather than inventing new vocabulary; class-only
                   framework reference ("multi-agent frameworks")
                   avoids per-framework claim risk.

                 - **Aligns with SOUL-A010** (Coherent Falsehood +
                   Anchor Obligation) — F041's three anchors
                   discharge the obligation A010 names for absolute
                   claims about reality in always-on doctrine.

                 - **Aligns with mind-rule-5** (never always-on past
                   the description budget) — the rewrite adds ~6–8
                   lines, well within the seed's existing budget;
                   it does not require auto-loading any new file or
                   referenced doctrine.

                 - **Aligns with seed §Naming Roles in the Moment**
                   (the Emissary moment at SOUL-098: evidence
                   overturned the prior belief about Soul's
                   relationship to tools; the amendment captures
                   the post-evidence belief in doctrine).

                 - **No contradictions with existing doctrine.** The
                   first paragraph of §External Skills and Tools
                   (line 191, "composes with other skill ecosystems")
                   is canonical and untouched. The Surface-when…
                   table's shape-match semantics are preserved
                   explicitly in the rewrite.

                 - **Framework-list naming decision: (c) class-only**
                   ("multi-agent frameworks") per Body decision at
                   SOUL-100. Rationale: the framing rewrite is about
                   the *layer relationship*, not about specific
                   frameworks; class-only avoids making per-framework
                   claims that would themselves need testing. This
                   means CrewAI/AutoGen are no longer named in the
                   seed — they were only named because the original
                   text made framework-specific claims; with those
                   removed, naming them is no longer necessary.

ACCEPTED BY:     Body, 2026-05-27 (via "continue" instruction at
                 SOUL-101 after reviewing the SOUL-099 chamber
                 synthesis and the SOUL-100 proposal. The Body's
                 four-decision acceptance at SOUL-100 ["accept,
                 reshape F041, framework-list (c), launch A016"]
                 plus the SOUL-101 "continue" together comprise
                 the acceptance signal. File moved from
                 `amendments/proposed/` to `amendments/accepted/`;
                 seed edit at `operations/CLAUDE.md:208` executed
                 in the same beat; F041 closed by this amendment).

FILED BY:        Revelator (the chamber's reframe — the README said
                 it all along; the slip is internal to the
                 project's canon);
                 Archaeologist (the field survey — no comparable
                 system uses peer-role-mapping);
                 Emissary (the reality test — BMAD's actual
                 architecture contradicts the seed; named per seed
                 §Naming Roles);
                 Seer (the timeline read — slip happened in the
                 same week as the section's adoption; over-
                 elaboration is the mechanism);
                 Steward (retire the sentence; elevate the README
                 framing in its place);
                 Skeptic (Tribune — tested R3 vs R1; tested
                 vocabulary-laundering; tested pattern-matching;
                 R3 survived);
                 Accountant (Tribune — fix-now economics);
                 Advocate (Tribune — adopter mental-model harm
                 prevention);
                 Guardian (Censor — verified NOT rubber-stamp);
                 Cartographer (Censor — mapped territory; surfaced
                 the framework-list naming question Body resolved
                 via (c)).

                 The amendment is a chamber-synthesis output;
                 every role above contributed a chamber statement
                 at SOUL-099 that this amendment carries forward.
```

---

## On Acceptance

This amendment closes a Coherent Falsehood in always-on doctrine — the kind of failure A010 was written to catch. The slip was 6 days old (§External Skills and Tools adopted 2026-05-21; this fix lands 2026-05-27). Three anchors discharged the Anchor Obligation: the project's own README + AGENTS.md, the agent-tool field's universal pattern, and BMAD's actual architecture.

The work lands in two places on acceptance:

1. `operations/CLAUDE.md:208` — one-paragraph replacement (1 → 2 paragraphs; ~6–8 line net growth). The full proposed text is in **WHAT CHANGES** above.
2. This amendment file moves from `amendments/proposed/` to `amendments/accepted/`.

On acceptance, **F041 closes** (its discharge path is this amendment). The reshape commit can fold F041's closure into the same commit as the seed edit.

**Forward-looking validation** (does NOT block acceptance): a second-framework Researcher beat (CrewAI's supervisor model OR AutoGen's GroupChat model) would *further validate* the chamber's structural conclusion that R3 hybrid generalizes across frameworks. Without it, A016's class-only framing ("multi-agent frameworks") still holds because it makes no framework-specific claims. The second-framework beat ripens as a follow-up idea, not a precondition.

**Open question:** at the next /soul-distill, consider whether the **phase-shaped vs posture-shaped** distinction (named in the chamber's BMAD anchor) and the **wrapping vs parallel-adoption** relation (R3) belong as candidate mind.md rules or contrast-cases. The Distiller decides at that refresh; not part of A016.
