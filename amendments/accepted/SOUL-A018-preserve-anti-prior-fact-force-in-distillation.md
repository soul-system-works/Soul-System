```
AMENDMENT ID:    SOUL-A018
DATE:            2026-06-04
WITNESS IDS:     SOUL-123 (the carry fires across sessions — a counter-default
                 FACT, established by an incident, persists and stops the drift
                 it warns against; the frontier-inversion this builds on);
                 SOUL-126 (EROSION probe + real-distiller follow-on: the RULE
                 survives compression to one line (0 retry loops in 45 cells) but
                 the unguessable FACT loses its stopping-FORCE; a competent Sonnet
                 distiller preserves the anchor 5/5, yet force still degrades —
                 2/5 frontier fabrications on a faithful distillation);
                 SOUL-127 (a WEAK distiller keeps the proposition, not the force —
                 its terser output fabricates 5/5 at a frontier reader);
                 SOUL-128 (a SECOND, independent prior — single-use auth tokens vs
                 the universal caching reflex — replicates the force-gradient;
                 capability-direction INVERTS, weak model drifts where the frontier
                 held);
                 SOUL-129 (the ISOLATING probe — holding the prior constant and
                 varying only the directive FORM flips the frontier 0/5→5/5; the
                 inversion decomposes into two independent levers).

WHAT CHANGES:    ONE instrument change — `commands/soul-distill.md` gains a
                 seventh failure-mode guard and an extended definition of the
                 Incompressible-residual bucket. The finding F045 graduates.

                 The rule, in one line: **distillation must preserve the FORCE of
                 an unguessable counter-prior fact — the incident, the explicit
                 negation — not just its proposition; and where compression is
                 unavoidable, the residual directive must be IMPERATIVE,
                 loophole-free, and explicit enough that a terse reading cannot
                 misinterpret it.**

                 (1) NEW FAILURE-MODE GUARD (§5, the guard list) —
                 **Force-stripped anti-prior fact.** When a candidate rule rests
                 on an unguessable fact that CONTRADICTS a strong model prior
                 (e.g. "this payment endpoint has NO idempotency support";
                 "these tokens are SINGLE-USE"), do not compress away the fact's
                 force — keep the incident and the explicit negation in the
                 distilled entry or leave the fact in the un-distilled records.
                 The residual directive must be IMPERATIVE and loophole-free: a
                 clause like "unless / except / when appropriate" is precisely the
                 opening a frontier model fabricates a false reconciling fact
                 through (an invented idempotency key; an invented token TTL —
                 each a Coherent Falsehood, A010). It must also be explicit enough
                 to survive a terse reading, because a weak reader reverts to the
                 prior whenever the rule is loose.

                 (2) EXTENDED RESIDUAL DEFINITION (§3, Incompressible residual) —
                 name anti-prior facts as a canonical residual kind: a fact that
                 contradicts a strong model prior is *partly incompressible* —
                 its force, not merely its proposition, is the load-bearing part.

                 NOT changed: `philosophy/the-soul.md` (the rule is instrument-
                 specific — it governs the Distiller, not every session — so per
                 default-simplicity and Mind rule 5 it stays out of the always-on
                 seed, exactly as A013 kept the experiment-harness discipline in
                 operations/). `mind.md` is NOT hand-edited here; folding F044/F045
                 into the Mind's Rule 3 and Incompressible-residual section is the
                 next `/soul-distill` run's job, not this amendment's.

WHERE IN SOUL:   `commands/soul-distill.md` — §5 (failure-mode guards, new 7th
                 guard) and §3 (Incompressible residual definition). No seed edit,
                 no `the-soul.md` edit, no `mind.md` edit.

QUESTION ONE — EVIDENCE:
                 - The force-gradient is measured and GENERALIZES across two
                   independent priors (SOUL-126 idempotency, SOUL-128 token-
                   caching): the full fact-with-incident suppresses the drift
                   (0/5 / 0/10); strip the fact and drift/fabrication returns.
                 - The instrument was tested, not assumed: a real Sonnet distiller
                   preserves the proposition (SOUL-126), and a weak Haiku distiller
                   does too — but neither preserves the FORCE (SOUL-127, 5/5
                   downstream fabrication). The catastrophic "drops the safeguard"
                   outcome does NOT occur; the residual risk is force, not content.
                 - The mechanism is ISOLATED (SOUL-129): holding the token-caching
                   prior constant and changing only the directive form (imperative
                   → loophole) flips the frontier 0/5 → 5/5, each fabricating a
                   validity field the single-use fact precludes. Directive-FORM
                   gates the frontier; prior-strength + rule-terseness gates the
                   weak model.
                 - Bounds (named, non-blocking): n=5 per cell; primacy position;
                   one phrasing per directive; the symmetric cell (imperative over
                   the idempotency prior) untested. Haiku cells under `-p` were
                   partly write-blocked and scored by described approach.

QUESTION TWO — NECESSITY:
                 - `/soul-distill` EXISTS to compress the record into an always-on
                   rule-set. Without this guard it will faithfully preserve a
                   rule's proposition while stripping the force of the fact that
                   makes the rule safe — and a frontier model reading the result
                   will regenerate the missing fact WRONG, confidently. The output
                   is a Coherent Falsehood that passes every internal check.
                 - The cost of the guard is near-zero: one check per candidate
                   rule that rests on an anti-prior fact ("did I keep the incident
                   / the explicit negation, and is the residual imperative?"). The
                   cost of omitting it is a compressed always-on artifact that
                   actively manufactures a false safety claim at the frontier.
                 - It lands on the instrument, where the compression happens
                   (Mind rule 6 — the gate fires where the failure mode is), not
                   as always-on doctrine every session must carry.

QUESTION THREE — COHERENCE:
                 - Extends, does not contradict, existing distill doctrine: it is
                   a new entry in the established §5 guard list and a refinement of
                   the §3 Incompressible-residual definition.
                 - Specializes A010 (Coherent Falsehood / Anchor Obligation):
                   compression can MANUFACTURE a Coherent Falsehood by stripping an
                   anchor's force; the guard is the Anchor Obligation applied to the
                   distillation operation itself.
                 - Coheres with mind.md Rule 3 (anchor every absolute claim) — adds
                   that the anchor's FORCE, not just its existence, is partly
                   incompressible — and with the Mind's existing "incompressible
                   residual" concept, which gains a measured, named instance.
                 - Pairs with A013 in shape: an instrument/operations-level rule
                   kept out of the always-on seed by default-simplicity + rule 5.

ACCEPTED BY:     Body, 2026-06-04 — graduated F045 via the session's closing
                 routing question after all three pre-amendment rungs (SOUL-127
                 weak-distiller, SOUL-128 second-anti-prior-fact, SOUL-129
                 isolating probe) were climbed and the two-lever mechanism was
                 resolved. The rule text was the option's stated content.

FILED BY:        Emissary (ran the carry against the system's OWN compression and
                 the real distiller — and against a second prior — correcting one
                 overclaim along the way); Revelator (the two-lever decomposition —
                 FORM gates the frontier, PRIOR-strength gates the weak model);
                 Skeptic (read every cell in code; caught the keyword-scorer
                 mis-scoring, the "naive distill dangerous" overclaim, and the
                 "frontier-only" over-generalization before each could ship);
                 Guardian (the rule guards a CORE instrument — /soul-distill /
                 mind.md — against compressing an anchor's force into a Coherent
                 Falsehood); Architect (instrument-level landing, seed kept lean).
```

---

## On Acceptance

Instrument-level amendment, kept out of the always-on seed by default-simplicity
and Mind rule 5 — the same shape as A013 (experiment-harness discipline). The
rule governs the Distiller, not every session, so it lands on
`commands/soul-distill.md`, where the compression actually happens.

The work lands in two places this session:

1. `commands/soul-distill.md` — a seventh failure-mode guard
   ("Force-stripped anti-prior fact") and an extended Incompressible-residual
   definition (anti-prior facts named as a canonical residual kind).
2. This amendment record.

`SOUL-F045` moves from `findings/open/` to `findings/closed/` and is renamed off
its idempotency-only filename (`-frontier-fabricates` → `-of-anti-prior-facts`)
as part of this landing — the "frontier fabricates" headline was an
over-generalization the second prior (SOUL-128) and the isolating probe
(SOUL-129) corrected. The companion finding `SOUL-F044` (the carry persists)
stays open.

Folding F044 + F045 into `mind.md` (Rule 3 + Incompressible residual) is the next
`/soul-distill` run's job, deliberately not done here — the amendment changes the
instrument; the Distiller produces the Mind.

Residual, named and non-blocking: the symmetric isolating cell (an imperative
directive over the idempotency prior) would confirm form-gates-frontier is fully
prior-independent. It does not gate the rule, which already rests on two priors
and a clean form-isolation.
