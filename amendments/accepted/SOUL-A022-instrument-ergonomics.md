AMENDMENT ID:    SOUL-A022
DATE:            2026-06-12
WITNESS IDS:     SOUL-167 (the v2.0.0 Body review session — the decisions this
                 amendment records); SOUL-163/164 (the universal session-boundary
                 capture loss — the evidence that capture must not depend on the
                 Body remembering a command); F044 (the measured triage rule the
                 model applies when proposing); F029 (the distribution model this
                 amendment extends to the plugin).
WHAT CHANGES:    Three instrument-ergonomics changes, all from the same Body
                 review observation: "it's not clear WHEN I should use these."
                 (1) /soul-capture becomes MODEL-PROPOSABLE for `idea` and
                 `witness` modes: the model may invoke the skill when it spots
                 an unguessable (the F044 triage rule), drafting the entry and
                 holding for the Body's confirmation before any append. The
                 `finding` mode stays Body-gated — graduation remains an earned,
                 explicit act (Rule 7 / I024 unchanged).
                 (2) /soul-distill becomes MODEL-PROPOSABLE on a growth signal:
                 when material record activity has accumulated since the `Last
                 distilled` stamp (guideline: ~15+ new witness entries, or any
                 finding closure / accepted amendment), the model proposes a
                 distill; on the Body's yes it runs and presents the draft.
                 Draft-for-curation and never-auto-commit are unchanged.
                 (3) The plugin becomes a COMPLETE install path: /soul-init
                 derives the Soul System root from its own skill location (which
                 resolves to the repo for symlink installs and to the plugin's
                 installed copy for plugin installs) and writes the @import
                 against that root. README leads with the plugin; clone becomes
                 the developer/contributor path.
WHERE IN SOUL:   skills/soul-capture/SKILL.md (frontmatter flag + proposal
                 protocol); skills/soul-distill/SKILL.md (frontmatter flag +
                 growth-signal proposal section, replacing the Manual-fire-only
                 paragraph); skills/soul-init/SKILL.md (root derivation);
                 README.md (plugin-first Quick Start).
QUESTION ONE:    Evidence — the capture gap is the program's most universal
                 result: in EVERY condition tested, a finding born mid-session
                 died at the session boundary unless an instrument captured it
                 (SOUL-163/164). An instrument that fires only when the Body
                 remembers a command inherits the Body's memory as its weakest
                 link; the Body's own review states the link is weak ("not clear
                 WHEN"). The F044 triage rule is measured and written — the
                 model can apply it and the Body can veto.
QUESTION TWO:    Necessity — this REVERSES a documented decision: /soul-distill
                 was "Manual-fire only … deliberately deferred until the manual
                 cadence proves insufficient." The Body's review IS that
                 insufficiency arriving, on the system's own terms; the deferral
                 clause discharged exactly as written. Recorded here rather than
                 silently overwritten (contract rule 8: overrides happen out
                 loud).
QUESTION THREE:  Coherence — the Body remains the confirming authority on every
                 write: capture proposals hold for a nod, distill still never
                 auto-commits, finding stays Body-gated. The headless result
                 (record files written 0/8 in automation, v1-stream) is not
                 contradicted: nothing here writes without eyes on it; the
                 change moves the *initiative*, not the *authority*. The
                 fabrication axis (SOUL-164) is respected: model-proposed
                 entries pass through the same draft-shown-before-append
                 protocol that lets the Body catch invented content.
