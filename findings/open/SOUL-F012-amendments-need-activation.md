```
FINDING ID:      SOUL-F012
DATE:            2026-05-20
WITNESS IDS:     REF-09 VISUAL-024/025/026 + closing F003 ("reading a
                 lesson is not internalising it"); REF-08 continued retrospective
                 (1206 lines, ZERO mentions of "Universe Collapse"/"A003"/"v0.3"
                 despite the doctrine being loaded — concept re-derived as
                 "Coherent Falsehood"); both: outward reach was Body/expert-
                 triggered, never role-self-triggered.
WHAT:            The amendments are accurate but not causal. The doctrine
                 describes the right behavior; it does not make that behavior
                 fire. Loaded text does not become live vocabulary — especially
                 off-domain: REF-08 LOADED v0.3.0 (confirmed it found Universe
                 Collapse on resume) yet never used the term, re-deriving the
                 concept from the physics instead. By the Soul's own Ad Hoc
                 Methodology failure mode ("if it must be requested it has not
                 been internalized"), the amendments are not yet internalized.
WHY NOT YET AMENDMENT:  More text will not fix a text-doesn't-fire problem —
                 adding an amendment here would repeat the failure. This is the
                 Artificer's summons by the Soul's own definition ("summoned when
                 the Witness log reveals that a role is failing to activate
                 reliably, that a decision point consistently goes unguarded").
                 The fix is an activation INSTRUMENT (harness hooks, a forced
                 pre-completion gate), not prose. Needs a Council decision on what
                 minimal instrument to build, tool-agnostically.
FILED BY:        Archivist
RELATED:         [[SOUL-F001]] (ratchet-not-engine — same root), [[SOUL-F013]]
                 (the concrete firing rule), [[SOUL-A003]] (the amendment that
                 described without firing), [[SOUL-F014]] (ambition gate — same
                 activation root)
CONVENING:       2026-05-20. Recognized as the Ad Hoc Methodology failure mode at
                 the doctrine level, and as the Artificer's summons (per the Soul's
                 own definition). Judge: the fix is an instrument, not prose.
                 FIRST INSTRUMENT BUILT — commands/soul-verify.md (installed to
                 ~/.claude/commands/): a forced, invokable pre-completion gate
                 that operationalizes A005 (global invariant), F013 (external
                 anchor for absolutes), A003 (outward reach), A007 (capture +
                 inspect), F016 (honest markers). This is the PORTABLE, Body-
                 invoked form — a partial fix. The auto-firing form (a Claude Code
                 Stop hook that fires without Body invocation) is the stronger
                 upgrade, deferred to a Body decision and to hooks/.
STATUS:          Open — partial fix shipped (/soul-verify). Stays open until the
                 gate AUTO-fires without Body invocation (the Stop-hook upgrade).
                 That is the true close of the activation gap.
```

The deepest result of the whole dogfood program: the doctrine evolved to
*describe* reality well (every amendment was independently re-derived by the
work) but does not yet *drive* behavior without the Body. The leveraged move is
no longer more amendments — it is an activation layer.
