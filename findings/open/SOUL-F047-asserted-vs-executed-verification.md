```
FINDING ID:      SOUL-F047
DATE:            2026-06-05
WITNESS IDS:     RESEARCH-N-001 (reference project: RESEARCH-N — the witness lives in that repo's
                 .soul/witness.md, not here; cited as the anchoring instance)
WHAT:            An ASSERTED anchor-check is not an EXECUTED one, and the gap is invisible
                 from inside the record. A prior session on the `RESEARCH-N` project recorded, in
                 research-state.yaml, "every number cross-checked vs the experiment JSONs -
                 exact match." A later review that actually RE-EXECUTED the check — recomputing
                 each paper table value and headline figure from the source-of-truth JSONs —
                 found one cell unverifiable against the data (a coverage number printed 0.408;
                 the value computable from the JSON was 0.142). It was a Coherent Falsehood: it
                 survived because it was plausible, sat in a finished table, and was shielded by
                 the earlier "all verified" assertion. The earlier assertion was ITSELF a
                 Coherent Falsehood — a claim of completed verification that had not been fully
                 performed. The Anchor Obligation says any claim about reality needs an external
                 anchor; this sharpens it: "I have verified all N numbers" is itself a claim
                 about reality, it is cheap to assert and expensive to execute, so it
                 under-executes for the same activation reason F014's expansion roles under-fire
                 and F037's Body-input goes unasked — the responsible-feeling cheap move
                 (re-reading the prose record of a past check) crowds out the costly correct one
                 (mechanically recomputing from source). The corrective is RE-EXECUTION at the
                 completion gate, not a second reading: when a prior record asserts "verified,"
                 the gate re-derives from source rather than trusting the assertion.
WHY NOT YET AMENDMENT:  One anchoring instance (RESEARCH-N-001). To graduate to doctrine it needs
                 (a) >=2-3 more instances where a recorded "verified / checked / matches" claim
                 fails on re-execution, across different work, showing the asserted-vs-executed
                 gap recurs and is not a one-off; (b) a crisp TRIGGER — which completion claims
                 warrant mechanical re-execution vs. legitimate trust (all numeric tables? only
                 load-bearing numbers? the cost scales with N, so a blanket "re-verify
                 everything" would punish honest records and bloat every gate); and (c)
                 reconciliation with SOUL-F012 (the completion gate fired this session, but on
                 the SHIP claim, not on the prior VERIFICATION claim) and the Anchor Obligation,
                 so it adds a "verification claims are anchorable claims — re-execute, do not
                 re-read" sub-clause rather than duplicating either. Candidate landing once
                 earned: a sub-clause of the Anchor Obligation / completion-gate doctrine, not a
                 new role.
FILED BY:        Emissary (took the recorded "verified" belief back to reality, where it
                 failed for one cell), Skeptic (named the asserted-vs-executed gap), Guardian
                 (named the activation-gap kinship to F014/F037 and the completion-gate seam).
RELATED:         [[SOUL-F012]], [[SOUL-F014]], [[SOUL-F037]]
STATUS:          Open
```
