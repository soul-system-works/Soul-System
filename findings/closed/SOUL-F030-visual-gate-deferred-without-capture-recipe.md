```
FINDING ID:      SOUL-F030
DATE:            2026-05-22
WITNESS IDS:     SOUL-063 (cross-project harvest); blog reference-project
                 .soul/events.jsonl (repeated soul.gate.completion.flagged, F012 hook).
WHAT:            ACTIVATION observation from greenwoodms06.github.io — a reference/dogfood
                 project that imports the seed. Across several turns the agent claimed
                 visual work complete on a GREEN BUILD ALONE: a repo/demo button restyle, a
                 new project page with an embedded architecture SVG, and two hand-authored
                 SVG card icons. The F012 completion hook fired check #4 ("Visual artifact
                 captured & inspected, not deferred?") every time — and every time the agent
                 ANSWERED it in prose ("GAP → rendered layout not visually eyeballed")
                 rather than DISCHARGING it, shipping with the look deferred to the live
                 site / the Body's eye. The gap only genuinely closed on the icons, when the
                 agent rasterized the SVGs with sharp (the project's OWN dependency) and Read
                 the PNGs back — confirming the boiling curve and the Universe→Body→Soul
                 nesting were correct. So A007 ("visual/non-automatable Witness → capture +
                 inspect") is doctrinally present, F008 already named the category, and the
                 hook even NAMES the check — yet capture-and-inspect still defaulted to
                 deferral until a concrete, low-friction capture method was reached for.
WHY NOT YET AMENDMENT:  Not a doctrine change — A007 mandates capture+inspect; F008 named
                 the category. This is an ACTIVATION + TECHNIQUE refinement in the F012
                 family: the gate DETECTS the deferral but supplies no METHOD, so the path
                 of least resistance is acknowledge-and-ship. Candidate operational fix:
                 (a) pair check #4 in completion-gate.md / soul-verify.md with a default
                 capture recipe for non-rendered artifacts — "rasterize SVG/markup via the
                 project's own image lib and Read it; screenshot a page via the run/verify
                 skill" — so "inspect" has a one-step instantiation independent of the
                 Body's eye; (b) treat a prose "GAP → not eyeballed" on a VISUAL change as a
                 NON-PASS, not a clean line — it is an admission the obligation was skipped.
FILED BY:        Archivist (cross-project harvest from the blog reference project)
RELATED:         [[SOUL-F008]] (the category — visual / non-automatable Witness),
                 [[SOUL-A007]] (the capture+inspect amendment this fails to activate),
                 [[SOUL-F012]] (amendments-need-activation — the hook that fired here),
                 [[SOUL-F009]] (artifact discipline erodes at scale — same drift family).
STATUS:          Closed [2026-05-26, via SOUL-066]. Operational fix landed: hook check #4
                 + soul-verify.md check #4 now pair with the default capture recipe and
                 mark "not eyeballed" as NON-PASS; F031's split (design-capture vs target-
                 tool render) folded in. Indexing slip noted in witness: F030 referenced
                 "check #4 in completion-gate.md," but that file's #4 is Advocate/Skeptic
                 — the visual check lives in the hook + soul-verify.md, where the fix went.
```
