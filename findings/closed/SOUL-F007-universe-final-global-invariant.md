```
FINDING ID:      SOUL-F007
DATE:            2026-05-20
WITNESS IDS:     REF-07 (§"Universe-final"; +200 K validation bias that
                 all internal unit tests masked; energy-balance global invariant
                 caught it); heat HEAT-006 (analytical ground truth); REF-08
                 docs/VALIDATION.md (verification != validation)
WHAT:            Universe consultation has teeth only where an external referent
                 exists, and "tests pass" is internal coherence dressed as truth.
                 REF-08's defining lesson: a +200 K model bias passed every
                 unit test and was caught only by a global energy-balance
                 invariant and benchmark validation. Verification (does it
                 compute consistently) is not validation (does it match reality).
                 The doctrine's "consult the Universe before complete" did not
                 distinguish these.
WHY NOT YET AMENDMENT:  Candidate: a Universe-final gate — before calling work
                 complete, verify a GLOBAL invariant or external referent, not
                 just local tests. Strong on problems with ground truth (heat,
                 REF-08); unclear how it applies where no external referent
                 exists (visual). Council must define what counts as a global
                 invariant across domains.
FILED BY:        Archivist
RELATED:         [[SOUL-F008]] (visual Witness — domains without ground truth)
STATUS:          Closed [resolved by SOUL-A005, 2026-05-20]
```
